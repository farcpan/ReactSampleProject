import React from 'react';
import { Button } from '@material-ui/core';

const TestComponent = () => {
    return (
        <div>
            <Button>React Test Component</Button>    
        </div>
    );
}

export default TestComponent;
